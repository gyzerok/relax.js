'use strict';

module.exports = function (grunt) {

    /*grunt.config.set('copy', {
        app: {
            files: [
                { expand: true, cwd: 'src', src: ['**/*', '!app/**'], dest: 'dist' }
            ]
        }
    });

    grunt.loadNpmTasks('grunt-contrib-copy');*/
};